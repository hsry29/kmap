/**
 * 영구 파트너(제휴) 매장 — 앱 배포 시 모든 사용자에게 적용.
 *
 * 관리자 패널에서 추가한 항목은 우선 브라우저(localStorage)에만 저장됩니다.
 * 모든 사용자에게 영구 반영하려면, 관리자 패널의 "Copy partners (JSON)" 버튼으로
 * 복사한 내용을 아래 entries 배열에 붙여넣고 다시 배포하세요.
 *
 * 각 항목 형식:
 *   {
 *     key:  'name:광야@서울'        // 또는 'kakao:<장소ID>' (안정 식별자)
 *     name: '광야@서울'             // 패널·마커 표시용 이름
 *     lat:  37.544455, lng: 127.044301,  // 지도 마커 좌표
 *     perk: '주차 2시간 무료 · 쿠폰 발급'  // 상세창에 강조될 혜택 문구
 *   }
 *
 * 참고: src/data/places.js 의 큐레이션 장소에 isPremium:true 로 지정한 곳은
 *       별도 등록 없이 자동으로 파트너로 취급됩니다(빌트인).
 */
export const ADMIN_PARTNERS = {
  entries: [],
}
